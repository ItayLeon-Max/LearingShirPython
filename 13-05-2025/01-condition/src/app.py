# Condition & Operator / Logic operators


# START_PRICE = 10.2
# PRICE_PER_KM = 1.30
# PRICE_PER_BAG = 2.0
# bag = int(input("enter how many bag: "))
# get_km = float(input("enter the km: "))
# total_price = float(START_PRICE + (PRICE_PER_KM*get_km) + (float(bag)*PRICE_PER_BAG))
# print(total_price)

DELAY_PER_FLOOR = 5
TRAVEL_TIME_BETWEEN_FLOORS = 3
get_floor = int(input("enter the floor: "))
total_time = DELAY_PER_FLOOR + (TRAVEL_TIME_BETWEEN_FLOORS * get_floor)
print(f"total time is: {total_time} seconds")







